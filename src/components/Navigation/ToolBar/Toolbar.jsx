import React from 'react';
import Logo from '../../Logo/Logo'
import './Toolbar.css'

const toolbar = props => (
  <header className="toolbar">
    <div >MENU</div>
    <Logo />
    <nav>
      <ul>...</ul>
    </nav>
  </header>
);

export default toolbar